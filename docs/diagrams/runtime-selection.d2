direction: right

ExecuteRequest -> DefaultRuntime -> "Profile selection"

Backends: {
  label: "Backends"
  style: { border-radius: 8; shadow: true }
  "unsafe host"
  docker
  kubernetes
  gvisor
  firecracker
  wasm
}

"Profile selection" -> Backends."unsafe host"
"Profile selection" -> Backends.docker
"Profile selection" -> Backends.kubernetes
"Profile selection" -> Backends.gvisor
"Profile selection" -> Backends.firecracker
"Profile selection" -> Backends.wasm
